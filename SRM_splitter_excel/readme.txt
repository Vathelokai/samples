This MS Excel module imports a CSV file (SRM.txt), converts it to a spreadsheet, standardizes the data, then splits the sheet into 'one workbook per region'.  It exports each workbook into the output folder.

This is done manualy by the customer on an as-needed basis.
